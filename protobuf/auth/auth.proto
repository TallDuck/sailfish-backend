syntax = "proto3";

package auth;

service Auth {
  rpc Authenticate(Request) returns (Response);
  rpc Invalidate(Request) returns (Response);
}

message Request {
  string token = 1;
}

message Response {
  bool status = 1;
}